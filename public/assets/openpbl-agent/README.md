# OpenPBL companion character assets

Generated from the user-approved standard view and independently approved normalized action frames in `output\openpbl-sprite-maker\banxue-xiaoling-motion-c`. The packager only publishes actions whose deterministic and visual QA both pass; it preserves unrelated existing atlases during partial runs. Rebuild with `node scripts/package-openpbl-agent-assets.mjs output\openpbl-sprite-maker\banxue-xiaoling-motion-c`. Set `NEXT_PUBLIC_AGENT_ART=legacy` to use the previous body assets.
