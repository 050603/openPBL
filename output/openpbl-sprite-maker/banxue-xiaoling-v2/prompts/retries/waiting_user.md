Retry only the complete `waiting_user` action strip for 伴学小灵.

Use the attached canonical base `canonical/canonical-base.png` and layout-only guide `references/layout-guides/waiting_user.png`. Preserve the standard-view identity: deep blue-gray rounded body, off-white face/belly, page ears, sprout and glowing dot, short limbs, same face construction, and the same #2E6BCB replaceable scarf in every frame.

Generate exactly 4 distinct, separated, full-body frames left-to-right on a perfectly flat #FF00FF chroma-key background. Keep scale, x registration, feet/seat baseline, materials, lighting, and scarf structure stable. Fix the previous action-specific failure while preserving all already-passing identity constraints.

Action semantic: 期待地等待用户回应。
Do not use: ordinary idle, text, speech bubble, background residue, checkerboard, scene, floor, shadow, glow, speed line, dust, motion blur, detached effects, text, labels, guide marks, cropped parts, or replacement eyes.
