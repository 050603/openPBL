Retry only the complete `error` action strip for 伴学小灵.

Use the attached canonical base `canonical/canonical-base.png` and layout-only guide `references/layout-guides/error.png`. Preserve the standard-view identity: deep blue-gray rounded body, off-white face/belly, page ears, sprout and glowing dot, short limbs, same face construction, and the same #2E6BCB replaceable scarf in every frame.

Generate exactly 4 distinct, separated, full-body frames left-to-right on a perfectly flat #FF00FF chroma-key background. Keep scale, x registration, feet/seat baseline, materials, lighting, and scarf structure stable. Fix the previous action-specific failure while preserving all already-passing identity constraints.

Action semantic: 困惑、失败或暂时无法继续。
Do not use: red X, floating punctuation, detached smoke, background residue, checkerboard, scene, floor, shadow, glow, speed line, dust, motion blur, detached effects, text, labels, guide marks, cropped parts, or replacement eyes.
