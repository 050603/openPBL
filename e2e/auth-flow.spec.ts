import { test, expect } from "@playwright/test";

/**
 * Auth flow E2E skeleton.
 *
 * Verifies the teacher login / logout journey end-to-end.
 *
 * Status: skeleton — implementation deferred until seed data + auth
 * fixtures are stable. Skipped via `test.skip` to keep CI green.
 */

test.skip("teacher can log in and reach the dashboard", async () => {
  // TODO: implement after auth seed fixture is available.
});

test.skip("teacher can log out from the dashboard", async () => {
  // TODO: implement after auth seed fixture is available.
});

test.skip("unauthenticated users are redirected to /login", async () => {
  // TODO: implement after auth seed fixture is available.
});
