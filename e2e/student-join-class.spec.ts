import { test, expect } from "@playwright/test";

/**
 * Student joins a class E2E skeleton.
 *
 * Covers: student signs in → enters class code → lands in classroom →
 * sees the active stage → submits a response.
 *
 * Status: skeleton — implementation deferred until seed data and
 * classroom-stage selectors are stable. Skipped via `test.skip`
 * to keep CI green.
 */

test.skip("student can join a class with a valid code", async () => {
  // TODO: implement after classroom seed fixture is available.
});

test.skip("student sees the active stage after joining", async () => {
  // TODO: implement after classroom seed fixture is available.
});

test.skip("student can submit a response to the current stage", async () => {
  // TODO: implement after classroom seed fixture is available.
});
